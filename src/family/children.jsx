import React from 'react';
import Child from './child'

const Children = ({}) => {
    return(<div className='children'>
        children
        <Child></Child>
        <Child></Child>
        <Child></Child>
        </div>
    );

}
export default Children